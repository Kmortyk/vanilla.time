package iss.vanilla.time.page;

import android.support.v7.widget.RecyclerView;
import android.view.View;

public interface OnSnapPositionChangeListener {
    void onSnapPositionChange(View snapView, int position);
}
